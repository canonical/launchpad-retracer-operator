server {
    server_name _;
    listen 80 default_server;
    listen [::]:80 default_server;

    root /srv/retracers;

    absolute_redirect off;

    location / {
        autoindex on;
        index index.html;
        try_files $uri $uri/ =404;
    }
}
